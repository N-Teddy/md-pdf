# Accessibility + Internationalization

## PDF/UA Support
- V3.0: semantic HTML structure + bookmarks
- V3.1: tagged PDF validation workflow
- Enforce alt text for images by default

## Tagged PDF Structure
- Heading hierarchy, lists, tables, and figure tagging
- Optional outlines for chapters/sections

## RTL + CJK Handling
- RTL layout and text direction support
- CJK font fallback with per-profile font lists
- Language-aware typography settings

## Hyphenation
- Hyphenation via plugin (language packs)
- Optional soft-hyphen insertion for long words

## Font Fallback Strategy
- Per-profile font stacks with fallbacks
- Default bundles for Latin + CJK
- License-safe font bundles only
